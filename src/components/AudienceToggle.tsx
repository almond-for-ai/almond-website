// Audience toggle removed — renders nothing.
// Almond is now presented as a universal memory layer (no Solo/Team distinction).
export function AudienceToggle(_props?: {
  className?: string;
  variant?: "light" | "dark";
  size?: "md" | "sm";
}) {
  return null;
}
